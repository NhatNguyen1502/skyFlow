akka {
  # tắt log trong test để output gọn
  loglevel = "OFF"
  stdout-loglevel = "OFF"
  log-dead-letters = off
  log-dead-letters-during-shutdown = off
  actor.warn-about-java-serializer-usage = off
}
